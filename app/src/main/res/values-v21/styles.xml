<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="style_bottomNavigation">
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:background">@color/Transparen</item>
        <item name="android:textColor">@drawable/tabbar_fontcolor</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>

        <!--定义分割线的样式-->
        <item name="android:listDivider">@drawable/list_bg</item>
    </style>

    <style name="ClassifyBntStyle">
        <item name="android:button">@null</item>
        <item name="android:background">@color/Transparen</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>
    <!--AS3.1以上要取消按钮的默认样式必须要把背景设置为透明-->
    <!--<item name="android:button">@null</item>-->
    <!--<item name="android:background">@color/Transparen</item>-->
</resources>
